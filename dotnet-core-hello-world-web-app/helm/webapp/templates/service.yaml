apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-webapp.fullname" . }}
  labels:
    {{- include "dotnet-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "dotnet-webapp.selectorLabels" . | nindent 4 }}

